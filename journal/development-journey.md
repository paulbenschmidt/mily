# Development Journey

- 2025-10-04:
    - I'm a little surprised that I discovered why the font was so light this quickly. When I was working on it last night, I noticed that the font for some inputs was so light that it was hard to read. This morning, I noticed that the font was back to dark. I went to Mac system settings and toggled the Light/Dark modes and saw the text switching back and forth. I defined the text color manually, and added a setting to `globals.css` to make dark mode defined by CSS selectors instead of system settings.
- 2025-10-03: Worked on this for a while. It was enjoyable going file by file to make sure that the styling still looked good and loaded properly.
- 2025-10-02: Man, this was humbling. I've been beating my head against the wall for hours trying to fix an issue with infinite redirects. I thought the issue could be related to: Next URL rewrites or requests not including tokens (as a result of Next URL rewrites). After moving slowly through the code, cleaning up some revisions, I tested it on an incognito browser and it worked! My browser had been caching the redirect responses (and the `Network` tab in Chrome DevTools was showing the redirect responses being cached). I'm glad I'm passed that. :) (Sidenote: this was frustrating because Claude kept circling through the same resolutions, so we kept going around and around. It's tough to say how much of the fault was mine, but I think I'm at fault.)
- 2025-09-29:
    - I finally realized what the issue on the frontend deployment was: a darn environment variable. Turns out I was just confusing the top-level collection of projects with the actual Mily project deployment. I managed to get the login for an existing user to work (and successfully grab the events for that user), but the user signup didn't work.
    - OH MY GORSH! Setting up the Porkbun `mily.bio` domain to point to the Vercel deployment URL was so stinking easy and very fast. I remember when I tried to do this for Mentorigami, it took me a few days. I finished this in a matter of minutes. Also, I didn't realize that `www.mily.bio` is actually a subdomain of `mily.bio`. Funky. Both are pointing to the same Vercel deployment URL. Vercel also automatically handles SSL certificates, which also took me a while to figure out back then.
    - The steps involved to set up the DNS Records for both Vercel and Resend (domain: `verify.mily.bio`) were to go to Porkbun and add the following records:
        - A Record: mily.bio -> Vercel IP grabbed the project's domain
        - CNAME Record: www.mily.bio -> Vercel URL grabbed from the project's domain after entering a new subdomain of `www`
        - TXT Record: send.verify.mily.bio -> Resend Domain Key provided during Resend setup
        - TXT Record: resend._domainkey.mily.bio -> Resend Domain Key provided during Resend setup
        - TXT Record: _dmarc.mily.bio -> Resend Domain Key provided during Resend setup (helps prevent being marked as spam)
    - In chatting with Claude, it recommended using a different email provider for sending emails. I was thinking I'd want all emails to go through Gsuite. I also didn't know that having emails coming from a different subdomain would be better for website reputation. Also, Claude mentioned that I wouldn't need to add an MX record for Resend because I didn't plan on receiving emails from Resend.
- 2025-09-27:
    - Big day! I deployed the backend to Railway. I'm glad that I have Django Admin because I was able to verify that that the backend was working as expected. The first few hours were spent trying to figure out the ideal MVP deployment infrastructure. In the end, I landed on Railway for the backend and Vercel for the frontend. I might migrate the database from Neon onto Railway, but I'm not sure yet.
    - Ran into issues on the frontend. First it was deployment issues that required modifying Next.js settings. After I solved those, I ran into CORS issues and then local storage of cookies (apparently also caused by having separate servers for front and backend). So I'm going to transition to JWTs. I'm pretty brain-dead. As an aside, having CICD automatically available for both Railway AND Vercel is pretty great.
- 2025-09-21: Learned about sessions
    - Django sessions are created when calling the `login()` function. The backend database then stores the `_auth_user_id`, `_auth_user_backend`, and `_auth_user_hash` in the database and passes along the `sessionid` cookie to the frontend, which is a look-up key for the session in the database. This way credentials are never sent to the frontend, and browser only needs to send the `sessionid` cookie to the backend.
- 2025-09-19: Learned about CSRF and cookies
    - CSRF (Cross-Site Request Forgery) attacks exploit the trust that a website has in a user's browser. CSRF tokens must be explicitly included in requests, and malicious sites cannot access these tokens due to Same-Origin Policy restrictions - whether they're stored in cookies, HTML, or accessed via JavaScript. Same-Origin Policy restrictions are enforced by the web browser (not by the server) and are automatically enforced unless using side-stepped with a custom browser or browser extension.
    - Cookies are automatically included in the request (unlike CSRF tokens). These can be sent with every request, and they are automatically included in the request headers; however, they cannot be read by another website (due to Same-Origin Policy restrictions). In other words, SOP prevents reading responses, not requests.
    - Cookies solve the problem of authentication/session management ("Who is this user?") while CSRF tokens solve the problem of request authenticity ("Did this request really come from my application?").
    - There are two main levels of defense: the browser and the server.
        - The browser enforces the Same-Origin Policy (covered above) which prevents the browser from reading the request from another website. However, requests can still be sent from another website, which is why...
        - The server, by default, should reject/block requests that come from other websites; however, with a CORS (Cross-Origin Resource Sharing) policy, you can allow certain websites to interact with the server.
        - CSRF tokens straddle the browser and server since the browser cannot read CSRF tokens from other websites (due to Same-Origin Policy restrictions) and the server validates that the CSRF token is valid.
- 2025-09-18: Debugging session spent understanding CSRF tokens and cookies a little more. I was reunning into a backend server error on the logout view, and I finally realized that it was because POST requests to the logout view were not including the CSRF token (even though I specified `@csrf_exempt` as a decorator). I had Claude 4 write up a simple frontend function that included the CSRF token in the request headers, and it succeeded! I need to spend a little more time understanding CSRF and cookies, but I'm getting better.
- 2025-09-17: Finally figured out how to persist logged-in sessions across browser refreshes! Woohoo! I spent the initial 20 minutes or so reading about Django's session middleware, and then I went on to dig into the code. I initially thought the problem was with the frontend not sending the appropriate data to the backend; that was true (however, the issue wasn't actually on the frontend). After scattering logs throughout the frontend and eventually the backend, I realized that the essential `request.session` was empty, and then once I found that out, Claude code was quick to realize that this was because I wasn't calling `login(request, user)` with the `login` view, which meant that the necessary params like _auth_user_id, _auth_user_backend, and _auth_user_hash weren't being set. After adding that in, the sessions worked like a charm! Very proud of figuring that out. Also, it's nice that I'm being a little more sparing with my AI development. Since I'm realizing that token burn/cost is a real problem for some of the heavier models like Claude 4, I'm becoming better about digging into the code to understand WHERE it might be failing, then asking AI about the best way to solve that narrow problem (instead of forcing it to identify the issue, too). I think, in time, after seeing lots of implementations, I'll understand the better ways to solve the problems, too. That said, it's so nice that AI can keep updated knowledge of the latest package features—so I don't have to.
- 2025-09-16: A little bit of a frustrating development session. I was able to figure out why the white screen was appearing (it has to do with session authentication NOT being persisted after refreshing the browser); however, I was not able to figure out why this happens. I did learn lots of valuable things like: Django session middleware automatically looks up the session in `django_sessions` to see whether or not the session is still valid. I also was running up against my credit limit for Claude AI model (realized I was using Claude 4 which uses 2x credits). I'm a little grateful that it costs money to use AI because that'll force me to do some thinking for myself (like today) and also force me to craft better prompts.
- 2025-09-15: Ran into an issue where I was getting a 403 error when trying to create a new user. It turns out that Django throws funky behavior when authenticated via Admin with one user and trying to create another user via the frontend.
- 2025-09-14: After the app continually pushed an "authenticated" user back to the login page after refreshing the page, I spent the hour trying to figure out why. There were settings that needed configuring, but I also learned that my browser was preserving the Clerk cookies, so I needed to clear my cookies.
- 2025-09-13: I had a minor freak-out moment where I thought that the choice to have a separate back-end was stupid, and that I was concerned about security in the integration between front-end and back-end. However, after chatting about it,with Claude, I remembered the reason I chose the separate back-end was to easily support mobile development in the future, with the same back-end API.
- 2025-09-01: Spent the morning walking through all the code that Windsurf generated. I think that this is a VERY valuable exercise: let AI generate the boilerplate code and then walk through it slowly to make sure that I understand it and that it isn't doing any extra work. As part of this walkthrough, I simplified the database schema and started playing with some pytest tests.
- 2025-08-30: Set up front-end with Next.js and Clerk. Very impressed with Claude Sonnet's ability to write clean, bug-free code.
- 2025-08-02: Learned about Windsurf and rule setup (as well as model costs); created initial database schema in Django models.py
